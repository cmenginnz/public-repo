version: "3"  # optional since v1.27.0
services:
  nginx:
    image: nginx:alpine
    volumes:
      - ./configs/${PORTAINER_EDGE_ID}:/git-edge-id
      - ./configs/${PORTAINER_EDGE_GROUP}:/git-edge-group
    
